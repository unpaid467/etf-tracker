{
  "name": "market-monitor",
  "version": "1.0.0",
  "type": "module",
  "description": "ETF & Stock Market Monitor with Investment Simulator",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "build": "esbuild public/js/app.js --bundle --format=iife --outfile=public/js/bundle.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
